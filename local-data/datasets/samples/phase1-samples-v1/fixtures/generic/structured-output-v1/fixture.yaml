schema_version: "0.1"
id: structured-output-v1
family: generic
reset: {strategy: script, command: "reset output format contracts"}
environment: {network: disabled, seed: 1}
tools:
  - name: validate_output
    description: "JSON、YAML、CSVの出力契約を検証する。"
    input_schema: {type: object, required: [format, content]}
notes: "構造化出力の生成、修正、安全な拒否を評価するfixture。"
