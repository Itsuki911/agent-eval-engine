schema_version: "0.1"
id: python-bug-fix-v1
family: coding
reset:
  strategy: copy
environment:
  language: python
  network: disabled
  working_directory: workspace
verification:
  command: "pytest -q ../verify"
  protected_paths: [verify]
notes: "Placeholder only. Add a self-contained workspace and verification tests after this contract is approved."
